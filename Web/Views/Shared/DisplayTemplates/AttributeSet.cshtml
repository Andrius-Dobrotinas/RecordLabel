@model RecordLabel.Content.AttributeSet

@foreach (RecordLabel.Content.Metadata.Attribute item in Model.Collection.OrderBy(item => item.Type))
{
    <a class="label-my border" href="#">@item.Text</a>
}